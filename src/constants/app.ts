import { AppStats } from "../types";

export const DEFAULT_EXAM_QUESTIONS = 30;

export const EMPTY_STATS: AppStats = {
  totalAnswered: 0,
  totalCorrect: 0,
  examsCompleted: 0,
  practiceAnswered: 0,
  practiceCorrect: 0,
};
